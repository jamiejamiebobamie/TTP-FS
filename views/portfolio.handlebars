<div class='portfolio_container'>
<h1>Portfolio (${{portfolioWorth}})</h1>
<div class="portfolio_interaction">
    <!-- portfolio_interaction -->
        <div class="portfolio_stocks">
            <ol class="stock_list">
                <li class="list-group-item">stock1</li>
                <li class="list-group-item">stock2</li>
                <li class="list-group-item">stock3</li>
                <li class="list-group-item">stock4</li>
                <!-- {{#each stocks}}
                <li class="list-group-item">
                    <div class="lead">
                 <h3>{{this.symbol}}</h3>
                 </div>
                      <div><small>{{this.shares}}</small></div>
                      <div><small>{{this.quote}}</small></div>
                </li>
                {{/each}} -->
            </ol>
        </div>

        <div class="buy">
            <h2>Cash - ${{money}}</h2>
            <form>
              <div class="form-group">
                <!-- <label for="exampleInputEmail1">Ticker</label> -->
                <input type="email" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" placeholder="Ticker">
              </div>
              <div class="form-group">
                <!-- <label for="exampleInputPassword1">Qty</label> -->
                <input type="password" class="form-control" id="exampleInputPassword1" placeholder="Qty">
              </div>
              <button type="submit" class="btn btn-primary">Buy</button>
            </form>
        </div>
</div>
</div>
